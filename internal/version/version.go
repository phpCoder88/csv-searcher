// Package version contains global variables for app version, build date and commit.
package version

var (
	// Version return the release version.
	Version = "unknown"
	// BuildDate returns the build date.
	BuildDate = "unknown"
	// BuildCommit return the build commit.
	BuildCommit = "unknown"
)
